# 쌍곰 챗봇 설정 가이드

## 1단계: Gmail 앱 비밀번호 발급

1. Google 계정 접속 → **보안** 탭
2. **2단계 인증** 활성화 (필수)
3. 검색창에 **"앱 비밀번호"** 검색
4. 앱 선택: **기타(직접 입력)** → "쌍곰챗봇" 입력
5. 생성된 **16자리 비밀번호** 복사 (예: `abcd efgh ijkl mnop`)

---

## 2단계: .env 파일 설정

프로젝트 폴더에서 `.env.example` 파일을 복사해 `.env`로 저장 후 내용 수정:

```
GMAIL_USER=발송용이메일@gmail.com
GMAIL_PASSWORD=앱비밀번호16자리
COMPANY_NAME=쌍곰
SERVER_BASE_URL=https://배포후주소.onrender.com
```

---

## 3단계: Render 무료 배포

### 3-1. GitHub에 코드 올리기
1. https://github.com 접속 → 새 저장소 생성 (비공개 권장)
2. 프로젝트 폴더 전체를 업로드
   - `.env` 파일은 **절대 업로드 금지** (비밀번호 포함)

### 3-2. Render 서버 생성
1. https://render.com 접속 → 무료 회원가입
2. **New → Web Service** 클릭
3. GitHub 저장소 연결
4. 설정값 입력:
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn app:app`
   - **Plan**: Free

### 3-3. 환경 변수 입력 (중요!)
Render 대시보드 → **Environment** 탭에서:
```
GMAIL_USER         = 발송용이메일@gmail.com
GMAIL_PASSWORD     = 앱비밀번호16자리
COMPANY_NAME       = 쌍곰
SERVER_BASE_URL    = https://내앱이름.onrender.com
```

### 3-4. 배포 완료 확인
브라우저에서 접속:
```
https://내앱이름.onrender.com/health
```
아래와 같이 나오면 성공:
```json
{"status": "ok", "products": 92, "total_files": 424}
```

---

## 4단계: 카카오 i 오픈빌더 설정

### 4-1. 챗봇 생성
1. https://i.kakao.com 접속
2. **새 챗봇 만들기** → 이름 입력 (예: "쌍곰 승인서류")
3. 카카오톡 채널 연결

### 4-2. 스킬 등록
1. 좌측 메뉴 **스킬** → **새 스킬 추가**
2. 스킬 URL: `https://내앱이름.onrender.com/webhook`
3. 저장

### 4-3. 폴백 블록에 스킬 연결
1. **시나리오** → **폴백 블록** 클릭
2. **봇 응답** → **스킬 데이터 사용** 체크
3. 위에서 만든 스킬 선택
4. 저장

### 4-4. 배포
1. 우측 상단 **배포** 버튼 클릭
2. 카카오톡 채널에서 테스트

---

## 5단계: 테스트

카카오톡에서 채널 추가 후:
1. 아무 메시지 입력 → 품목 검색됨
2. "세라픽스" 입력 → 관련 품목 목록 표시
3. 품목 선택 → 이메일 주소 입력 요청
4. 이메일 입력 → 자동 발송 완료

---

## 품목 매핑 업데이트 (홈페이지 파일 추가/변경 시)

```bash
python scraper.py
```

실행 후 `document_map.json` 파일이 갱신됩니다.
Render에 재배포하면 자동으로 반영됩니다.

---

## 문제 해결

| 증상 | 원인 | 해결 |
|---|---|---|
| 이메일 발송 안 됨 | 앱 비밀번호 오류 | Gmail 앱 비밀번호 재발급 |
| 품목 검색 안 됨 | 품목명 매핑 없음 | `python scraper.py` 재실행 |
| 서버 오류 | 환경변수 미설정 | Render Environment 탭 확인 |
| 링크 만료 | 24시간 초과 | 고객이 재요청 |
